
---
const { title, link, excerpt, featured_img } = Astro.props;
---

<div class="card">
  <div class="card-body">
    <img src={featured_img} alt={`Featured image for the article ${title}`} />
    <div>
      <h2><a href={link}>{title}</a></h2>
      {excerpt}
    </div>
  </div>
</div>